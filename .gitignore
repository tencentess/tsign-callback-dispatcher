node_modules/
dist/
.env
.env.*
!.env.example
logs/
config/versions/
config/operation-logs.json
config/users.json

# WAL (Write-Ahead Log) 运行时数据
data/
*.log
*.tsbuildinfo
.DS_Store

# IDE & tools
.dev-pids/
.playwright-mcp/

# Lock files (各子项目独立管理)
package-lock.json

# K8s secrets (NEVER commit plaintext credentials)
k8s/secret.yaml

# K8s test manifests (local testing only)
k8s-test/

# Docker test artifacts
docker/test/k6/results/
.codebuddy/
